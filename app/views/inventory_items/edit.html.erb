<div class="text-center clearfix" style="margin-bottom: 3em">
  <%= link_to 'Back', inventory_items_path, {class: 'btn btn-default pull-left'} %>
  <h3 style="display: inline" class="text-center">Editing item "<%= @inventory_item.productName %>"</h3>
  <!--<%= link_to 'Edit', edit_inventory_item_path(@inventory_item), {class: 'btn btn-primary pull-right'} %>-->
</div>

<%= render 'form', inventory_item: @inventory_item %>
